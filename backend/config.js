//port that the backend uses as a destination for the frontend to communicate with
export const PORT = 5555;
//this is the URL of the Mongo database, this could be local as well, but dynamic backup abilities arise with cloud providers such as "Atlas"
export const mongoDBURL = 'mongodb://127.0.0.1:27017/community-partners';